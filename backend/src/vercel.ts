// Vercel serverless entry. Used when this backend is deployed as Vercel functions.
// The Express app is exported and given to Vercel via @vercel/node.
import app from './app.js';

export default app;
